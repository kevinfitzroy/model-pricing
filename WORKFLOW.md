# WORKFLOW — 事实性核验工作流(通用方法论)

> 这是一套**与具体题材无关**的事实性核验流程。任何"需要把外部世界的事实/数据/声明,整理成可对比、可追溯、不含幻觉的结构化结论"的任务,都套用它。
> 具体题材叫一个 **case**(见 [`cases/`](cases/));本文件是所有 case 共享的"宪法"。
> **本文件允许演进**,但任何对规则/契约的改动都要在 [`CHANGELOG.md`](CHANGELOG.md) 留痕。

---

## 0. 适用场景 & 核心问题

适用于:收集 API 价格、核验一篇文章里的事实断言、核某产品的官方规格、核一条新闻/传闻……

这些任务表面是"查资料",真正的难点是同一个:**LLM 极容易把听起来合理的内容当成事实输出(幻觉)**。本工作流存在的唯一目的,就是用**流程**把幻觉挡在产出之外:

> **首要约束:宁可少、宁可留洞,绝不编造。** 一个没有来源的结论,比一个空格更糟。

---

## 1. 三阶段(define → collect → audit)

### 第一步 · 定义(每个 case 必须先做)
把这个 case 怎么实例化工作流写成 `cases/<case>/CASE.md`:**被核验单元是什么、什么算可信来源、产出长什么样、验收标准是什么**。定义先于执行。

### 第二步 · 执行(取证 → 落盘 → 派生)
1. **核实"清单/范围"**:要核验哪些单元(哪些模型/哪些断言/哪些规格),本身就要联网确认,不靠记忆(R1)。
2. **逐项取证**:对每个单元,抓**第一手/官方来源**,把原文**逐字**存进 `evidence/`(带 URL + 抓取日期)(R2/R3)。
3. **派生结构化结论**:据证据整理进结构化文件(JSON/CSV),**每个结论反向引用证据**,未知留空并显式标记(R4/R5/R6)。
4. **分析层另放**:对比、排名、判断、解读写进单独文件,明确标注"这是解读"(R7)。

> **回路**:第二步发现第一步没预料到的情况(新维度/新断言类型),**回头改 CASE.md 并记 CHANGELOG**。第一步不冻结。

### 第三步 · 审计(独立角色)
另起一个**独立 reviewer**(人或 agent)按 [`REVIEW.md`](REVIEW.md) 逐条核验,**独立重抓抽样**,出 `REVIEW_REPORT.md`。前两步留痕越清楚,这步越快。

---

## 2. 八条硬规则(R1–R8,违反即判审计不通过)

- **R1 — 联网核实,不信记忆。** 要核验什么、对象的精确身份/现状,**必须来自联网检索到的一手信息**,不得凭训练知识脑补(新事物训练知识里常没有或记错)。
- **R2 — 证据先行,无证据不落结论。** 任何写进结构化产出的事实,先得有一条**逐字引用的原文证据**;产出里每个结论都要能反向指到某条证据。
- **R3 — 一手/官方为准。** 以**官方页 / 一手来源 / 原始文献**为第一来源;聚合站、二手转述、他人观点**只能用于交叉验证**,不能作唯一来源,且必须标 `secondary`。
- **R4 — 精确到身份。** 记录对象要精确(精确 model id / 精确断言原文 / 精确产品型号+版本),不用泛称冒充。
- **R5 — 未知显式,不猜。** 查不到就标 `unknown` / `needs_verification` 并留空,**绝不用估算/推测填空冒充已知**。
- **R6 — 原文进证据,加工在派生层。** 证据层保留来源**原始形态**(原文、原单位、原币种、原口径);任何换算/归一化/解读只发生在派生层,且换算规则写明。
- **R7 — 事实与分析物理分开。** 带引用的事实是高可信资产;对比/排名/判断/"谁对谁错"是低可信派生观点,**必须放在单独文件/区块并标注"这是解读"**。事实层不准混入未经核对的结论。
- **R8 — 冲突不静默。** 多个来源打架时,**都记下来 + 标 `conflict`**,在 notes 写清分歧与取舍依据(通常取一手),不私自抹掉一方。

---

## 3. 角色

- **Collector(执行方)**:做第二步。守 R1–R8,只负责"忠实取证 + 结构化",尽量不下结论。
- **Reviewer(审计方,独立)**:做第三步。默认**不信任** collector 的产出,逐条核、独立重抓,只出报告不改数据(或改动全部留痕)。
- 两个角色尽量由不同 agent 担任,保独立性。

---

## 4. 输出契约(磁盘布局,所有 case 一致)

```
cases/<case>/
  CASE.md            # 本 case 怎么实例化工作流(范围/单元/来源/产出/触发语)
  evidence/<源>/<日期>.md   # ⬅ 事实层:一手来源逐字原文 + URL + 抓取日期,不可篡改、不加工
  <data>.json        # ⬅ 派生层:结构化结论,每条带 evidence_ref + confidence(套 ../../schema/output.schema.json)
  <data>.csv         # ⬅ 派生层:人读视图(可选)
  analysis.md        # ⬅ 分析层:对比/判断/解读,明确标注"这是解读"
  REVIEW_REPORT.md   # ⬅ 第三步审计报告
  index.html         # ⬅ 可视化(可选)
```

通用结构化 schema 见 [`schema/output.schema.json`](schema/output.schema.json):每个"被核验单元"带 `subject`、`source_url`、`source_type`、`evidence_ref`、`confidence`/`verdict`、`notes`,字段值随 case 而定。

---

## 5. confidence / verdict 分类法

**confidence(数据可信度,所有 case 通用)**:
- `confirmed` — 一手来源逐字确认。
- `needs_verification` — 仅二手 / 有疑点 / 只取到区间。
- `conflict` — 多来源打架(R8)。
- `unknown` — 查不到,留空(R5)。

**verdict(断言核验类 case 专用,如文章/新闻核验)**:
- `supported` 支持(一手来源印证)· `refuted` 反驳(一手来源否证)· `misleading` 误导(部分真但断章/夸大)· `unverifiable` 无法证实。
- 每个 verdict 必须挂证据(R2),区分"事实断言"与"观点"(观点不判真假,只标记为观点)。

---

## 6. 怎么实例化一个新 case

1. 复制 [`cases/_TEMPLATE/CASE.md`](cases/_TEMPLATE/CASE.md) 到 `cases/<新case>/CASE.md`,填:被核验单元、可信来源定义、产出结构、触发语、可视化形式。
2. 按三阶段执行,守 R1–R8。
3. 在 [`cases/README.md`](cases/README.md) 登记这个 case + 它的触发语。

触发可走 `/factcheck` skill(见 [`cases/README.md`](cases/README.md)),或直接口述"用核验工作流处理 X"。
