# CASE — <case 名称>

> 本 case 套用 [`../../WORKFLOW.md`](../../WORKFLOW.md) 的通用方法论(三阶段 + 八条硬规则 R1–R8 + 输出契约)。
> 下面只写**本 case 的专属实例化**。复制本模板填空即可。

## 1. 目标 & 输入
- **一句话目标**:<这个 case 要产出什么>
- **输入**:<无 / 一个 URL / 一个对象名 / 一段陈述>

## 2. 被核验单元(unit)
- 一个"单元"是:<一个模型 / 一条事实断言 / 一项规格参数 / 一个传闻>
- 精确身份怎么记(R4):<精确 id / 逐字原句 / 型号+版本>

## 3. 什么算可信来源(R3)
- **一手 / 官方**:<官方定价页 / 原始文献 / 一手报道 / 厂商 spec 页>
- **二手(仅交叉验证)**:<聚合站 / 转述 / 评论文章>

## 4. 产出结构
- 套 [`../../schema/output.schema.json`](../../schema/output.schema.json);本 case 的 `items[].fields` 放:<列出字段>
- `verdict` 是否使用:<断言类 case 用;数据类 case 留 null>
- 人读视图:<csv / 表格 / 无>
- 可视化:<index.html 图表 / 无>

## 5. 范围边界
- 收:<…> 不收:<…>

## 6. case 专属注意 / 口径
- <换算假设、判定标准、易错点、已知陷阱>

## 7. 审计补充(给 reviewer,叠加在 REVIEW.md 通用清单之上)
- <本 case 特有的检查点,如该题材的官方域名表、判定 verdict 的标准>

## 8. 触发语
- `/factcheck <case> <输入>`
